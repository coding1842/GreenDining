<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ecom4.green.user.dao.UserDAO">
	<insert id="insertAddress" parameterMap="addressDTO">
		INSERT INTO ADDRESS(USER_ID,
							NAME,
							ADDRESS,
							ADDRESS2,
							ZIPCODE,
							REQUEST_OPTION,
							REQUEST_TEXT)
		VALUES( #{user_id},
				#{name},
				#{address},
				#{address2},
				#{zipcode},
				#{request_option},
				#{request_text})
	</insert>
	<select id="selectAddressList" parameterMap="addressDTO">
		SELECT USER_ID, NAME, ADDRESS, ADDRESS2, ZIPCODE, REQUEST_OPTION, REQUEST_TEXT
		FROM ADDRESS
		WHERE USER_ID = #{user_id}
	</select>
</mapper>